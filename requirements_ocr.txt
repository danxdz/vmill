# Core FastAPI dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0

# OCR and Image Processing
paddleocr==3.2.0
opencv-python-headless==4.8.1.78
# Python 3.13-compatible NumPy
numpy==2.2.6
paddlepaddle==3.2.0
# Explicit Paddle runtime deps (avoid resolver warnings)
networkx==3.6.1
opt_einsum==3.3.0
protobuf==7.34.0
safetensors==0.7.0

# HTTP and API
requests==2.31.0
httpx==0.25.2

# Image processing utilities
Pillow==11.2.1

# File handling
python-multipart==0.0.6

# PDF generation and processing
reportlab==4.0.7
PyMuPDF==1.26.4

# Excel generation
openpyxl==3.1.2

# Additional dependencies for HF Spaces
python-dotenv==1.0.0
pydantic==2.12.5
starlette==0.27.0
